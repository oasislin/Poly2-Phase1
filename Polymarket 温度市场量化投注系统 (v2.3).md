---
created: 2026-08-14 23:22:27
updated: 2026-08-14 23:22:27
---
# 项目方案更新：Polymarket 温度市场量化投注系统 (v2.3)
## 版本记录
- **v2.3**：[关键调整] 基于 **GEFS Reforecast 仅 00Z 起报** 的硬约束，重构 Phase 1 数据提取与模型训练策略，建立“季节×时效”分层建模框架，并优化实时推理映射机制。
- **v2.2**：数据源调整为 Wunderground，明确预测最高温与最低温概率分布。
---
## 一、 调整背景与原因说明
### 1.1 数据源硬约束发现
在 v2.2 方案执行过程中，通过实测 AWS Open Data 的 GEFSv12 Reforecast 数据集，发现与预期存在重大差异：
- **约束一（起报时间）**：GEFSv12 Reforecast 每天仅有一个起报时次（00Z），无 06Z、12Z、18Z 循环数据。
- **约束二（成员数）**：常规情况下仅提供 5 个集合成员（c00, p01, p02, p03, p04），每周三扩展至 11 成员（用于 +35 天预报）。
- **约束三（时间窗口）**：数据覆盖 2000-2019 年，时间分辨率为 3 小时（部分文件 3h 窗口未全覆盖，但 6h 窗口连续）。
- **约束四（空间分辨率）**：前 10 天预报为 0.25° 全球网格（721×1440），之后转为 0.5°。
### 1.2 对原方案的冲击
v2.2 方案假设训练数据可覆盖多个起报时次，以构建不同时效的训练样本。现实约束导致：
- **训练样本量减少**：无法像预期那样从 4 个起报时次获取样本，只能依赖单一 00Z 起报。
- **时效覆盖不均**：并非所有“理想时效节点”都有对应样本。例如，预测某城市最高温，从 00Z 起报，只有特定 Lead Time（如 7h, 31h, 55h）可对齐目标时间。
- **实时推理挑战**：实时 GEFS 有 00/06/12/18Z 四个起报时次，而训练模型仅基于 00Z，需解决“如何用 00Z 训练的模型处理 06Z/12Z/18Z 实时预报”的问题。
### 1.3 核心调整思路
**从“以起报时间为中心”转向“以目标有效时间为中心”**。利用 Reforecast 的长预报序列（0-384小时），从单一起报时刻（00Z）切片出不同时效的预报样本，构建基于 **“目标时间 + Lead Time”** 的训练集。同时，在推理阶段建立映射机制，使任意实时起报时刻的预报都能对应到已训练模型。
---
## 二、 方案调整详解
### 2.1 数据架构调整（针对 Phase 1）
#### 2.1.1 观测数据（真值）
- **来源不变**：Wunderground 历史页面。
- **处理新增**：需精确匹配目标时间（如每日最高温发生的具体 UTC 时刻）。对于历史数据，需根据站点时区和季节，统计最高温/最低温发生的典型时间窗口（如最高温通常在 UTC 07:00 左右）。
#### 2.1.2 预报数据（特征）
- **来源不变**：GEFS Reforecast（仅 00Z 起报）。
- **提取策略重构**：
  1. **固定起报时间**：只处理 00Z 起报的 GRIB2 文件。
  2. **目标时间对齐**：根据站点时区，确定最高温/最低温的目标时间窗口（如上海最高温目标时间定为 UTC 07:00）。
  3. **切片提取**：从 00Z 起报的预报序列中，提取目标时间点前后 3 小时的预报值，组成不同 Lead Time 的样本。
     - 例如，目标时间 UTC 07:00，可提取 Lead Time = 7h（D-0）、31h（D-1）、55h（D-2）的样本。
  4. **特征工程**：计算集合均值、集合标准差、分位数等，作为 EMOS 输入特征。
### 2.2 模型训练策略调整（针对 Phase 1）
#### 2.2.1 模型分桶策略
采用 **“季节 × 时效” 二维矩阵** 训练多个 EMOS 模型：
| 维度 | 说明 |
|------|------|
| **季节** | 4 个季节（DJF, MAM, JJA, SON） |
| **时效** | 根据实际可达的 Lead Time 节点分桶。例如，上海最高温可达时效：{7h, 31h, 55h}。最低温可达时效：{22h, 46h}。 |
- **模型文件命名**：`{StationID}_{Season}_lead{Hours}h.pkl`（如 `ZSPD_JJA_lead7h.pkl`）。
- **训练逻辑**：每个模型独立训练，优化其特定时效下的校准参数。确保短时效模型输出方差小，长时效模型输出方差大。
#### 2.2.2 样本构造流程
```python
def build_training_data(station, target_time_utc, season):
    training_samples = []
    for year in range(2000, 2020):
        for month in season_months[season]:  # 根据季节过滤
            # 从 Wunderground 获取真值（该月每日最高温/最低温）
            truth_data = get_wunderground_truth(station, year, month)
            
            # 从 GEFS Reforecast 提取 00Z 起报数据
            gefs_init_00z = get_gefs_reforecast_init_00z(year, month)
            
            for day in truth_data:
                # 计算该日目标时间的 Lead Time（相对于 00Z 起报）
                lead_time = calculate_lead_time(day, target_time_utc)
                
                # 仅处理可达时效范围内的样本
                if is_lead_time_reachable(lead_time):
                    # 提取对应时效的集合预报特征
                    features = extract_features(gefs_init_00z, day, target_time_utc)
                    label = truth_data[day]
                    training_samples.append((features, label, lead_time))
    
    # 按 Lead Time 分桶，分别训练模型
    return bucket_samples_by_lead_time(training_samples)
```
### 2.3 实时推理映射策略（针对 Phase 1 推理阶段）
#### 2.3.1 映射机制
实时 GEFS 有四个起报时次。当获取到一个非 00Z 起报的预报时：
1. 计算其 Lead Time = 目标时间 - 实际起报时间。
2. **归一化**：将 Lead Time 映射到最近的已训练模型 Lead Time 桶。例如，Lead Time=19h，映射到已训练的 22h 桶（最低温）或 7h/31h 桶（最高温，需插值）。
3. **方差修正**：若映射存在跨桶（如 19h 用 22h 模型），需根据时效缩短适当收缩输出方差（如乘以系数 0.9）。
#### 2.3.2 推理流程图
```
实时 GEFS 数据 (任意起报时间)
            |
            v
    计算目标时间对应的 Lead Time
            |
            v
    归一化到已训练模型 Lead Time 桶
            |
            |--- 精确匹配 ---> 调用对应模型
            |--- 近似匹配 ---> 调用最近模型 + 方差修正
            |
            v
    输出概率分布参数 (μ, σ)
```
---
## 三、 项目设计文件更新（Phase 1 部分）
### 3.1 模块一：数据架构（v2.3 更新版）
#### 3.1.1 观测数据（真值）
- **数据源**：Wunderground 历史页面。
- **更新要点**：
  - 增加对“目标时间窗口”的统计：需分析站点历史数据，确定最高温/最低温发生的典型 UTC 时间（作为模型预测的目标时间）。
  - 示例：上海最高温典型时间窗口为 UTC 07:00，最低温为前一日 UTC 22:00。
#### 3.1.2 预报数据（特征）
- **数据源**：GEFSv12 Reforecast（2000-2019）。
- **更新要点**：
  - 明确约束：仅使用 00Z 起报的数据。
  - 特征提取需基于“有效时间窗口”，从单一起报文件中切片不同时效的样本。
  - 空间插值仍采用双线性插值，但需注意 0.25° 与 0.5° 网格的边界处理。
### 3.2 模块二：动态概率预测模型（v2.3 更新版）
#### 3.2.1 第一层：静态基础模型
- **模型选择**：偏态 EMOS，按 **“季节 × 时效”分桶** 训练。
- **训练数据**：来自 GEFS 00Z 起报的不同时效样本。
- **特征输入**：集合均值、集合标准差、分位数等。
- **输出**：概率分布参数（μ, σ）。
#### 3.2.2 第二层：动态修正层（不变）
- 仍采用数学截断算法，利用实时实况修正概率分布。
#### 3.2.3 第三层：物理硬约束（不变）
- 仍基于 Wunderground 历史数据统计站点最大升温/降温速率。
### 3.3 模块三：验证引擎（v2.3 更新版）
#### 3.3.1 核心验收指标
- **CRPS 评分**：需分别验证不同时效模型的 CRPS，并确认 CRPS 随时效缩短而单调下降。
- **PIT 检验**：每个时效模型需独立通过 PIT 均匀性检验。
#### 3.3.2 新增验收：时效覆盖度分析
- 需产出报告，明确说明每个站点最高温/最低温可达的时效节点数量，以及推理阶段映射策略的合理性。
---
## 四、 实施路线图（Phase 1：v2.3 调整版）
### Step 1.1 数据管道搭建（调整）
- 拉取 GEFS Reforecast 数据：只下载 00Z 起报的 GRIB2 文件。
- 爬取 Wunderground 历史：同时分析最高温/最低温发生的典型时间窗口。
- 对齐站点信息：建立站点时区与目标时间窗口的映射表。
### Step 1.2 模型训练（调整）
- 实现基于“有效时间窗口”的样本切片逻辑。
- 构造不同时效的训练集，训练 EMOS 模型矩阵。
- 实现模型保存与加载机制，按“季节×时效”命名。
### Step 1.3 动态层开发（不变）
- 实现数学截断算法，计算动态概率修正。
### Step 1.4 验收测试（调整）
- 生成统计报告，包括各时效模型的 CRPS、PIT。
- 验证 CRPS 随时效单调下降的特性。
- 验证推理映射策略的合理性。
---
## 五、 总结
本次调整是在数据源硬约束下的必然修正。核心变化是从“多起报时次假设”转向“单一起报时次、多时效切片”的训练范式。这要求我们在数据提取、模型分桶和推理映射三个层面进行重构，但总体框架仍遵循 v2.2 的“静态基础+动态修正+物理约束”三层架构。通过此次调整，方案更加贴合实际数据情况，确保了 Phase 1 的可执行性，并为后续 Phase 2 的盘口投注奠定了更坚实的预测基础。

