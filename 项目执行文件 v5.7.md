---
created: 2026-08-14 17:15:00
updated: 2026-08-14 17:15:00
---

# 项目执行文件（Phase 1：高精度物理概率模型构建）v5.7
## 版本记录
- **v5.7**：**[核心细化]** 敲定时效分层训练协议的可执行细节——最高温/最低温名义目标时间、9+8 时效节点矩阵（68 模型/站）、6h 窗口 TMAX/TMIN 日极值特征（完全包含规则+安全边际）、5 成员集合对齐、集合统计量口径（均值/方差/成员极值，弃分位数与时间特征）、训练样本量确认、三级降级触发条件（硬+软）、CRPS 衰减统计检验验收、验证集双轨划分（单次留出 + 训练期滚动验证）。
- **v5.6**：引入 **"时效分层训练协议"**，废弃单一模型架构，改为"季节 $\times$ 时效"二维矩阵建模。修正 Lead Time 计算逻辑，确立全时效衰减验收标准。
- **v5.5**：引入数据量优化策略。
- **v5.4**：引入双重验收标准与时间墙隔离。
---
## 1. 核心算法修正：时效分层训练协议
### 1.1 物理与统计事实
GEFS 预报误差特征随 Lead Time 呈显著非线性变化：
- **长时效（如 48h）**：集合离散度 $\sigma_{ens}$ 大，均值存在系统性平滑，EMOS 参数需大幅修正方差。
- **短时效（如 12h）**：集合离散度小，确定性高，EMOS 参数更侧重均值校准。
**结论**：严禁使用单一静态模型处理不同时效特征。
### 1.2 建模架构：季节 $\times$ 时效二维矩阵
采用 **方案 A：时效分桶 EMOS（Lead-time Specific EMOS）**。
**模型矩阵定义**：
$$ M_{s, \Delta t} $$
其中 $s$ 为季节（DJF, MAM, JJA, SON），$\Delta t$ 为归桶后的时效节点。
**时效节点定义（v5.7 修正）**：
| 目标类型 | 名义目标时间（当地时间） | 时效节点集 | 节点数 |
| :--- | :--- | :--- | :--- |
| **最高温** | **15:00 LT**（上海 = 07:00 UTC） | {54, 48, 42, 36, 30, 24, 18, 12, 6} | 9 |
| **最低温** | **06:00 LT**（上海 = 前一日 22:00 UTC；丹佛跟随 DST） | {48, 42, 36, 30, 24, 18, 12, 6} | 8 |

> **说明**：最低温节点集缺 54h 是物理必然——最低温目标（前一日 22:00 UTC）比最高温目标（当日 07:00 UTC）早 9 小时，D-2 00Z 起报到最低温仅 46h（归桶 48）。
**模型文件命名规范**：
```text
{StationID}_{Season}_{Max|Min}_lead{Hours}h.pkl
例：ZSPD_JJA_Max_lead54h.pkl, ZSPD_JJA_Min_lead24h.pkl
```
**模型规模**：$4 \text{ 季节} \times (9 + 8) = 68$ 个独立模型文件/站点，2 站点共 **136 个**。
---
## 2. 数据提取与时间计算逻辑
### 2.1 Lead Time 标准计算公式
**严格遵循气象定义**：
$$ \text{Lead Time} = \text{名义目标时间} - \text{Init Time (模式起报时间)} $$
其中"名义目标时间"为站点固定气候学极值时刻（最高温 15:00 LT / 最低温 06:00 LT），仅用于**分桶**，不用于特征采样。
### 2.2 特征提取：6h 窗口 TMAX/TMIN 日极值（v5.7 核心修正）
**放弃 3 小时窗口**：实测 reforecast 的 3h 窗口仅覆盖半天（[0-3], [6-9], [12-15], [18-21] 等），缺失 [3-6], [9-12], [15-18], [21-24]，用其取日极值会系统性遗漏落在未覆盖窗口的峰值（如上海 17:00-20:00 LT 的午后峰值）。**6h 窗口（[0-6], [6-12], [12-18], [18-24]）完整铺满 24h**，且 TMAX/TMIN 是模式对连续温度曲线的窗口内诊断极值（实测验证 `TMAX[0-6h] ≥ TMAX[0-3h]` 处处成立），无窗口内采样偏差。
**特征定义（窗口纳入规则 = 完全包含 ⊆ 本地日，v5.7 修订）**：
$$ \text{DailyMax}_{D} = \max_{\text{6h 窗口} \subseteq \text{本地日} D} \text{TMAX}_{窗口} $$
$$ \text{DailyMin}_{D} = \min_{\text{6h 窗口} \subseteq \text{本地日} D} \text{TMIN}_{窗口} $$
> **窗口纳入规则**：仅纳入**完全落在本地日内**的 6h 窗口（而非仅相交），以彻底消除跨日边界歧义（相邻日窗口在物理时段上有重叠，若纳入相交窗口，最低温特征可能混入前一日/次日凌晨数据）。
>
> **安全边际验证（上海/丹佛全季节）**：被丢弃的时段 = 本地午夜后 offset mod 6 小时 + 午夜前 6 − (offset mod 6) 小时（共 6 小时，均为夜间/傍晚，日极值不落于此）：
>
> | 站点/季节 | 覆盖跨度 (LT) | 最低温可能区间 | 最高温 15:00 | 边距 |
> | :--- | :--- | :--- | :--- | :--- |
> | 上海 全年 | [02:00–20:00] | 04:20–06:50 | ✓ | ≥2.5h |
> | 丹佛 冬季 | [05:00–23:00] | 06:30–07:20 | ✓ | ~1.5h |
> | 丹佛 夏季 | [00:00–24:00]（4 窗口零丢弃） | 05:00–05:30 | ✓ | — |
>
> **新增城市覆盖告警（Task 1.3 实现）**：当新增城市时，自动用天文算法计算其全年日出时间，验证"覆盖跨度 ⊇ [日出−1h, 日出+0.5h]"；若不满足（如高纬夏季日出早于覆盖起点），输出警告并人工处理。
**数据源成员协议（v5.7 新增）**：
- **训练（reforecast）**：GEFS Reforecast v12，**5 成员（c00 + p01-p04）**（AWS 数据源实证仅存 5 成员，2005/2015/2019 验证一致）。
- **预测（realtime）**：GEFS Realtime，**取相同成员集合 c00 + p01-p04**，确保集合离散度口径与训练一致。
- **分辨率统一**：训练/预测均使用 **6h 窗口 TMAX/TMIN**（reforecast 与 realtime 均含该口径，天然一致，符合 v5.1 协议）。
**特征提取代码逻辑修正**：
```python
def get_model_and_features(gefs_data, init_time_utc, station, target_type):
    # 1. 物理修正：双线性插值 + 高程调整 (v5.2协议)
    # 2. 日极值特征：本地日窗口内 6h TMAX/TMIN 窗口极值（对集合成员统计）
    if target_type == 'max':
        daily_extreme = tmax_6h.sel(windows ⊆ local_day).max('window')
        nominal_target_local = station.max_temp_time   # 15:00 LT
    else:
        daily_extreme = tmin_6h.sel(windows ⊆ local_day).min('window')
        nominal_target_local = station.min_temp_time   # 06:00 LT
    features = ensemble_stats(daily_extreme)  # 集合均值/方差/成员极值 (5 成员)

    # 3. 计算精确 Lead Time（到名义目标时间）
    lead_time_hours = (nominal_target_local - init_time_utc).total_seconds() / 3600

    # 4. 离散化到标准桶 (54, 48, ..., 6)，加载对应模型
    lead_time_bucket = round_to_nearest_6h(lead_time_hours)
    season = get_season(nominal_target_local)
    model = load_model(f"{station.id}_{season}_{target_type}_lead{lead_time_bucket}h.pkl")

    return model.predict(features)
```
**示例：上海 D-day 最高温（名义目标 15:00 LT = 07:00 UTC）**
| 更新时间点 (UTC) | 起报时间 | 名义目标 (UTC) | 原始 Lead Time | 归桶 | 使用模型 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| D-2 00Z | D-2 00Z | D-day 07Z | 55h | **54h** | `lead54h.pkl` |
| D-2 06Z | D-2 06Z | D-day 07Z | 49h | **48h** | `lead48h.pkl` |
| D-1 00Z | D-1 00Z | D-day 07Z | 31h | **30h** | `lead30h.pkl` |
| D-1 12Z | D-1 12Z | D-day 07Z | 19h | **18h** | `lead18h.pkl` |

**示例：上海 D-day 最低温（名义目标 06:00 LT = 前一日 22:00 UTC）**
| 更新时间点 (UTC) | 起报时间 | 名义目标 (UTC) | 原始 Lead Time | 归桶 | 使用模型 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| D-2 00Z | D-2 00Z | (D-1) 22Z | 46h | **48h** | `lead48h.pkl` |
| D-1 00Z | D-1 00Z | (D-1) 22Z | 22h | **24h** | `lead24h.pkl` |
| D-1 12Z | D-1 12Z | (D-1) 22Z | 10h | **12h** | `lead12h.pkl` |
| D-1 18Z | D-1 18Z | (D-1) 22Z | 4h | **6h** | `lead6h.pkl` |

> **丹佛注**：最低温名义目标同为 06:00 LT（本地时间），丹佛实行夏令时，其 UTC 目标随 DST 自动平移（夏 12:00Z / 冬 13:00Z），训练/预测统一按"本地名义时刻"计算，口径一致。
### 2.3 集合统计量（v5.7 定义）
对 5 个成员（c00 + p01-p04）的日极值特征，模型输入采用以下统计量：
- **集合均值** $\mu_{ens}$：5 成员日极值的平均（均值校准锚点）
- **集合方差** $\sigma_{ens}^2$：5 成员日极值的方差（EMOS 标准输入，校准集合离散度）
- **成员极值范围**：5 成员各自日极值的 max / min（对尾部目标敏感，捕捉成员分歧）
**明确排除**：
- **分位数**（10/25/50/75/90）：5 成员下分位数跳跃大、噪声高，初期不采用（可选后期评估）
- **时间特征**（day-of-year 等）：季节分桶已覆盖主要季节信号，不引入（Q18 = A）
### 2.4 训练样本量与独立性（v5.7 确认）
每个 (站点, 季节, 时效桶) 的训练样本 = 19 年 $\times$ ~91 天/季 $\approx$ **1729 样本/桶**（每 target day 每桶恰好 1 个 init 对应，无重叠）。EMOS 参数仅 5-7 个，样本充足。
相邻日样本因天气持续性高度相关 $\rightarrow$ **有效样本量低于名义值**。接受此不独立性：
- 不额外做样本去重（会进一步削减 6h/12h 等样本偏少的桶）
- 通过参数正则化与三级降级兜底（见 2.5）
### 2.5 三级降级训练协议（v5.3 细化）
针对偏态/截断 EMOS 拟合的数值不稳定性，自动降级：
| 级别 | 模型 | 触发条件 |
| :--- | :--- | :--- |
| **Level 1** | 截断/偏态 EMOS（全参数，CRPS 解析解，标准高斯 EMOS 参数热启动） | 默认 |
| **Level 2** | 标准高斯 EMOS（去偏态） | **硬触发**：优化器失败 / 参数 NaN/Inf / 超迭代上限 |
| **Level 3** | 气候学分布（历史同期均值+方差） | **软触发**：训练集 CRPS 不优于低级别模型或气候学 |
**触发规则（Q17 = B）**：硬触发（数值异常）直接降级；软触发（CRPS 无改进）用验证集判断，比气候学差即降级。保证系统永不崩溃。
---
## 3. 验证引擎升级：全时效衰减验收
### 3.1 验收标准更新
废弃单一时效验收，改为 **CRPS 衰减曲线验收**。
**验收逻辑**：
1. 分别计算各时效节点的 CRPS 均值。
2. **统计检验断言（v5.7 修正）**：CRPS 随 Lead Time 缩短应**单调下降**。因有限样本存在抽样噪声，采用**配对统计检验**（如 Diebold-Mariano 检验或配对 t 检验）判定相邻时效 CRPS 差异是否显著：差异不显著视为持平通过；仅当出现**统计显著倒挂**（如 p < 0.05 且倒挂幅度超过容差）才判定模型训练失败。
3. **PIT 均匀性**：每个时效分桶必须独立通过 PIT K-S 检验（P-value > 0.05）。
4. **锐度（v5.4 延续）**：MPIW Ratio < 0.9（相对气候学窄 ≥10%）。
### 3.2 验证集划分（v5.7 双轨）
- **单次留出（主验收）**：训练集 2000-2018（拟合各时效 EMOS 参数），验证集 2019（绘制 CRPS 衰减曲线、PIT/MPIW 计算）。
- **训练期滚动验证（增强）**：在 2000-2018 内执行 Rolling-Origin 滚动验证（逐年滚动原点），提升有限样本下结论的可靠性。
- **铁律**：训练阶段严禁访问任何验证集数据（时间墙隔离）。
---
## 4. 量化交易策略联动
基于新架构，确立动态仓位管理策略。**本部分为模型设计意图说明，Phase 1 不实现**（Phase 2 引入盘口与资金管理）：
| 预测阶段 | Lead Time | 模型特征 | 交易策略 |
| :--- | :--- | :--- | :--- |
| **早期埋伏** | 48h - 36h | 方差 $\sigma$ 大，分布宽 | **寻找赔率极高的尾部风险**：轻仓埋伏盘口定价错误的"极端天气"选项。 |
| **中期调整** | 24h - 18h | 方差收敛，趋势确立 | **动态对冲**：根据新旧模型输出分布的差异，调整仓位方向。 |
| **临盘重击** | 12h - 06h | 方差 $\sigma$ 极小，确定性高 | **Kelly 准则重仓**：确定性优势最大时，利用高置信度分布重仓出击。 |
---
## 5. 数据量优化策略（v5.5 延续）
- **分片下载**：按年份 / 成员 / 时次分片，避免单次下载过大文件。
- **断点续传**：维护下载状态文件（年份、成员、时次），失败自动续传，MD5 校验完整性。
- **本地缓存**：下载后立即裁剪区域并删除原始全球文件，仅保留裁剪后数据。
- **增量更新**：仅下载新增数据。
---
## 6. 总结
v5.7 版本将时效分层训练协议细化为可执行规格：
1. **目标时间明确**：最高温名义目标 15:00 LT，最低温 06:00 LT（丹佛跟随 DST）。
2. **特征口径正确**：6h 窗口 TMAX/TMIN 日极值（放弃残缺的 3h 窗口），训练/预测统一。
3. **成员对齐**：训练（reforecast）与预测（realtime）均使用 c00 + p01-p04 共 5 成员。
4. **模型矩阵**：68 模型/站点（最高温 9 节点 + 最低温 8 节点）$\times$ 4 季节。
5. **验收科学**：CRPS 衰减用统计检验而非严格单调断言，叠加 PIT 逐桶检验与锐度约束。
6. **验证双轨**：2019 单次留出 + 训练期滚动验证，时间墙隔离。
7. **特征集合**：集合均值 + 方差 + 成员极值范围，弃分位数与时间特征（5 成员口径）。
8. **降级兜底**：三级降级协议（硬触发数值异常 + 软触发 CRPS 无改进）。
