---
created: 2026-08-11 15:27:31
updated: 2026-08-11 15:27:31
---

# 项目方案：Polymarket 温度市场量化投注系统 (v2.2)  
（数据源调整版：以Wunderground为真值，预测最高温与最低温概率分布）  
## 1. 项目目的与战略定位  
**核心目标**：构建针对Polymarket温度市场的**连续概率预测系统**，输出最高温与最低温的概率分布，用于投注决策。  
**战略分阶段原则**：  
- **Phase 1（当前）**：聚焦高精度物理概率模型，以Wunderground历史数据为真值，训练最高温与最低温的校准模型，不考虑盘口与资金管理。  
- **Phase 2（后续）**：在Phase 1验收后，引入盘口流动性分析、滑点控制与资金管理策略。  
## 2. 模块一：数据架构（低成本实现）  
采用**免费公开数据+Python开源工具链**，确保数据源与结算口径一致。  
### 2.1 观测数据（真值与历史统计）  
- **数据源**：Wunderground历史页面（如`https://www.wunderground.com/history/monthly/cn/shanghai/ZSPD/date/2026-7`）。  
- **用途**：  
  - 作为模型训练的**真值标签**（提取每日最高温、最低温）。  
  - 统计站点级历史升温/降温速率（物理硬约束参数）。  
- **获取工具**：Python `requests`+`BeautifulSoup`爬取页面，解析“Day High”与“Day Low”字段。  
### 2.2 预报数据（特征源）  
- **数据源**：GEFS（Global Ensemble Forecast System）。  
- **理由**：免费、开源、提供集合预报（表征不确定性），符合EMOS模型需求。  
- **获取工具**：Herbie库（从AWS Open Data拉取GRIB2文件）+`xarray`/`cfgrib`处理。  
### 2.3 实时数据（未来实战用）  
- **数据源**：METAR（机场实时报文）。  
- **处理逻辑**：对齐Polymarket结算站点，清洗原始报文（剔除超物理范围值），与Wunderground真值站点ID匹配。  
## 3. 模块二：动态概率预测模型（Phase 1核心交付）  
采用**“静态基础+动态修正+物理约束”三层架构**，同时预测最高温与最低温的概率分布。  
### 3.1 第一层：静态基础模型  
- **模型选择**：偏态EMOS（Ensemble Model Output Statistics），针对**最高温**与**最低温**分别训练。  
- **输入特征**：GEFS集合均值、集合标准差、分位数等。  
- **输出**：基于预报的先验概率密度函数（如高斯分布的μ和σ）。  
- **训练逻辑**：最小化CRPS（Continuous Ranked Probability Score），分别优化最高温与最低温的预测。  
### 3.2 第二层：动态修正层  
- **触发机制**：随时间推移（每小时）触发。  
- **修正逻辑**：利用数学截断算法，分别对最高温与最低温进行修正：  
  - 输入：当前实况$T_{now}$（最高温/最低温）。  
  - 计算：$P_{posterior} = P(X \ge L | X > T_{now})$（最高温）或$P(X \le L | X < T_{now})$（最低温）。  
- **优势**：毫秒级计算，无滞后，实时更新概率分布。  
### 3.3 第三层：物理硬约束  
- **目的**：解决实时数据脏与模型滞后问题，确保预测符合物理规律。  
- **逻辑**：基于Wunderground历史数据，统计站点在对应季节、时段的**最大升温/降温速率**（如上海夏季最大升温速率）。  
- **熔断规则**：若当前实况$T_{now}$与目标档位$L$的距离超过历史最大变动幅度，强制判定概率为0或1，覆盖模型输出。  
## 4. 模块三：验证引擎（Phase 1验收标准）  
用于客观评估Phase 1是否完成，严禁进入Phase 2。  
### 4.1 交付物标准  
- 预测结果文件：包含历史回测的时间、站点、最高温与最低温的PDF参数、Wunderground真值。  
- 统计校准报告：分别展示最高温与最低温的模型表现。  
### 4.2 核心验收指标  
- **PIT直方图**：检验最高温与最低温的概率分布是否均匀（金标准）。  
- **CRPS评分**：衡量最高温与最低温的概率预测综合准确度。  
- **Talagrand图**：检验集合离散度是否合理。  
- **对比基准**：模型必须统计显著优于“朴素预报”（如直接使用GEFS均值）。  
**判定规则**：只有当模型在历史回测中通过上述指标，才允许启动Phase 2的资金投入。  
## 5. 实施路线图（Phase 1：数据基建与模型构建）  
### Step 1.1 数据管道搭建  
- 拉取GEFS历史预报数据（Herbie+AWS Open Data）。  
- 爬取Wunderground历史页面，解析每日最高温与最低温（requests+BeautifulSoup）。  
- 对齐站点信息（如ZSPD对应上海浦东国际机场）。  
### Step 1.2 模型训练  
- 实现基础版EMOS模型，分别训练最高温与最低温的校准模型。  
- 调优参数（如偏态系数、权重）。  
### Step 1.3 动态层开发  
- 实现数学截断算法，分别计算最高温与最低温的$P_{posterior}$。  
- 统计站点级物理硬约束阈值（如上海夏季最大升温速率）。  
### Step 1.4 验收测试  
- 生成统计报告，判定是否达标（PIT、CRPS等指标）。  
## 6. 关键调整说明  
- **数据源变更**：从ISD改为Wunderground，解决“结算口径不一致”问题，确保模型标签与Polymarket结算一致。  
- **模型目标扩展**：同时预测最高温与最低温的概率分布，覆盖Polymarket所有温度盘口。  
- **输出形式明确**：概率分布（非单一数值），直接用于计算投注概率（如$P(T_{max} \ge 35^\circ C)$）。  
此方案在保持原有“低成本、物理模型为核心”的框架下，通过数据源与模型目标的调整，更贴合Polymarket的实际结算需求。
