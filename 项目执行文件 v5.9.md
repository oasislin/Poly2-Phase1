---
created: 2026-08-15 11:27:35
updated: 2026-08-15 12:22:52
---
# 项目执行文件（Phase 1：高精度物理概率模型构建）v5.9.0
## 版本记录
- **v5.9**：**[算法深度修正]** 基于真实 GRIB2 索引文件验证，修正特征提取逻辑（弃 3h 选 6h 窗口），解决“伪极值切片”、“集合方差坍塌”与“方差人工缩放”三大算法死穴。引入自然日窗口聚合、气候学方差 Floor、模型参数时空插值三项核心修正。明确三级降级、极端事件验收、裁剪流程与成员对齐策略。
- **v5.8**：适配 Reforecast 单一 00Z 起报约束，确立时效分桶架构。
---
## 1. 核心算法修正：三大死穴的解决方案
### 1.1 第一死穴：伪极值切片 -> 自然日窗口极值聚合
**问题**：之前试图切片单一时刻（如 07:00 UTC）作为日最高温特征，忽略了峰值发生时间的不确定性（如雷阵雨提前降温）。
**数据源实证**：`.idx` 文件证实存在 `0-6 hour max fcst` 字段。
**修正方案**：
- **特征定义**：特征不再是单点温度，而是 **自然日内所有有效 6h 窗口 TMAX 的最大值**。
  $$ \text{Feature}_{\text{DailyMax}} = \max(TMAX_{0-6}, TMAX_{6-12}, TMAX_{12-18}, TMAX_{18-24}) $$
- **物理意义**：这完全对齐了模式对“日最高温”的诊断逻辑，消除了峰值时间错位风险。
### 1.2 第二死穴：5 成员集合坍塌 -> 气候学方差 Floor
**问题**：仅 5 个成员计算的样本方差 $S_{ens}$ 抽样误差极大，易导致 EMOS 模型“学废”方差参数。
**修正方案**：
- **成员对齐（核心约束）**：训练与推理阶段强制统一口径，仅使用 **c00 + p01-p04** 这 5 个成员。周三的11成员数据（训练集）和实时的31成员数据（推理集）均**只取此5个成员**，严禁混用。
- **方差公式修正**：在 EMOS 连接函数中引入气候学方差下限。
  $$ \sigma_{model}^2 = c^2 + d^2 \cdot S_{ens}^2 + \mathbf{\sigma_{clim}^2(\text{Season})} $$
  其中 $\sigma_{clim}$ 为站点季节历史标准差。确保即使集合坍塌（$S_{ens} \approx 0$），模型仍保有物理不确定性。
### 1.3 第三死穴：方差人工缩放 -> 模型参数时空插值
**问题**：对缺失时效节点（如 48h）采用“方差乘系数”破坏概率校准。
**修正方案**：
- **参数插值**：对缺失时效节点，在其相邻已训练节点的 EMOS 参数 $(a, b, c, d)$ 之间进行线性插值。
- **验证闭环**：插值生成的虚拟模型必须通过 PIT 均匀性检验。
---
## 2. 数据提取与时间计算逻辑（v5.9 最终版）
### 2.1 数据源解析协议（基于 IDX 实证）
- **变量分离**：TMAX 与 TMIN 存储于独立文件（`tmax_2m` vs `tmin_2m`），需分别下载。
- **起报时间锁定**：文件名格式 `YYYYMMDDHH_member.grb2`，其中 `HH` 固定为 `00`（仅 00Z 起报）。
- **成员清单**：必须下载完整集合 `{c00, p01, p02, p03, p04}` 以构建 5 成员统计量。
### 2.2 特征提取：6h 窗口极值锁定
**核心逻辑**：利用 GRIB2 中自带的 `X-Y hour max fcst` 变量，放弃瞬时温度切片。
**步骤**：
1.  **字段筛选**：仅提取 `0-6`, `6-12`, `12-18`, `18-24` 等 6 小时步长的 TMAX/TMIN 变量。**剔除所有 3h 变量**。
2.  **自然日对齐**：将 UTC 时间转换为站点本地时间（LT），筛选出 **完全落入目标自然日** 的 6h 窗口。
3.  **集合统计**：在筛选出的窗口上，对 5 个成员分别取极值，再计算集合均值 $\mu_{ens}$、方差 $\sigma_{ens}^2$ 及成员极值范围。
### 2.3 Lead Time 计算与时效节点映射
**计算公式**：
$$ \text{Lead Time} = \text{名义目标时间} - \text{Init Time (00Z)} $$
- 名义目标时间：最高温 15:00 LT，最低温 06:00 LT。
**时效节点表（最终版）**：
| 目标类型 | 可达时效节点 | 训练模型数/季 |
| :--- | :--- | :--- |
| **最高温** | `{54h, 30h, 6h}` | 3 |
| **最低温** | `{48h, 24h}` | 2 |
**实时推理映射**：
- 若实时时效为 48h（最高温缺失节点），调用参数插值算法：
  `Model_48h = Interpolate(Model_54h, Model_30h)`
### 2.4 区域裁剪与插值流程（澄清）
1.  **下载与初步裁剪**：下载全球 GRIB2 文件后，立即裁剪保存为站点周边 **21×21（约5°×5°）区域**，并删除原始文件。
2.  **训练/推理插值**：从保存的 21×21 区域数据中，定位站点位置，提取其 **周边 2×2（4个最近格点）** 进行双线性插值和高程修正。此最小邻域插值法是行业标准，与前期裁剪区域大小无冲突。
---
## 3. 模型训练协议：EMOS 公式定义（v5.9）
### 3.1 模型架构（Phase 1 最终选择）
- **分布族**：**标准高斯分布**。废弃偏态/截断分布，以确保5成员下的参数稳定性。
- **连接函数**：
  $$ \mu = a + b \cdot \bar{T}_{ens} $$
  $$ \sigma^2 = c^2 + d^2 \cdot S_{ens}^2 + \sigma_{clim}^2(\text{Season}) $$
### 3.2 三级降级训练协议（保留并强化）
| 级别 | 模型 | 触发条件 |
| :--- | :--- | :--- |
| **Level 1** | **标准高斯 EMOS + 方差 Floor** | 默认。 |
| **Level 2** | 气候学分布（历史同期均值+方差） | **硬触发**：Level 1 优化失败 / 参数 NaN/Inf / CRPS 显著差于气候学。 |
> 说明：方差 Floor 是 Level 1 内部的统计修正；Level 2 是模型训练失败后的系统级兜底。两者共同保障系统永不崩溃。
### 3.3 训练样本与成员处理
- **样本量**：每个 (站点, 季节, 时效桶 ) 约 1729 样本。
- **成员统一**：无论训练集（含周三11成员）还是推理集（含31成员），**只取 `c00, p01-p04`** 计算 $\mu_{ens}$ 和 $S_{ens}$。
---
## 4. 验证引擎：三重验收机制
### 4.1 标准节点验收（真实节点）
- 对已训练节点，执行严格的 PIT 均匀性检验与 CRPS 评分。
- **CRPS 衰减检验**：验证 CRPS 随时效缩短而下降（允许统计波动）。
### 4.2 插值节点验收（虚拟节点）
- **新增验收项**：在验证集上模拟生成插值节点（如 48h），检验其 PIT 直方图。
### 4.3 极端事件压力测试（替代 MPIW Ratio）
- **验收项**：抽取历史气温 TOP 10% 的极端日，验证模型给出的预测分布是否覆盖了观测真值（即 $P(T_{obs})$ 不低于预期）。
- **标准**：模型在极端日上的平均 CRPS 不劣于气候学基准，且 PIT 检验通过。
---
## 5. 数据工程实现细节
### 5.1 文件下载清单（以 ZSPD 站为例）
对于每个历史日期（2000-2019），需下载以下 5 个文件：
```text
tmax_2m_YYYYMMDD00_c00.grb2
tmax_2m_YYYYMMDD00_p01.grb2
tmax_2m_YYYYMMDD00_p02.grb2
tmax_2m_YYYYMMDD00_p03.grb2
tmax_2m_YYYYMMDD00_p04.grb2
(同理下载 tmin_2m 系列)
```
### 5.2 内存与缓存策略
- **流式读取**：利用 `cfgrib` 后端和 `index` 文件快速定位 `6h max` 字段。
- **降维裁剪**：下载后立即裁剪至站点周边 21×21 网格，删除原始全球文件。
---
## 6. 总结
v5.9 版本是对气象数据物理特性的深度适配与工程严谨性的终极平衡：
1.  **物理正确**：从“猜时刻”转为“取窗口极值”，根治了极值切片顽疾。
2.  **统计严谨**：通过方差 Floor 和参数插值，修补了小样本集合带来的统计漏洞。
3.  **工程健壮**：三级降级与严格成员对齐，确保系统在任何数据条件下均可运行。
4.  **验收闭环**：极端事件压力测试替代窄度指标，直指交易核心风险。

