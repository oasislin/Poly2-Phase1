# 三级降级改为两级降级

v5.7 的三级降级（偏态 EMOS → 标准高斯 EMOS → 气候学）中，中间层「标准高斯」的职责是「偏态解不出时退化」。v5.9 废弃偏态后，标准高斯即顶层模型，解不出或效果差时自然直接退回气候学，中间已无「更简化的参数化模型」可过渡。

因此 Phase 1 执行两级降级：Level 1 = 标准高斯 EMOS + 方差 Floor（默认）；Level 2 = 气候学分布（兜底）。硬触发 = 优化器超迭代 / 参数 NaN/Inf；软触发 = 验证集 CRPS 显著劣于气候学。参数越界（如 |d|>10）仅告警、不强制降级。
