---
created: 2026-08-20 11:00:00
updated: 2026-08-20 11:00:00
---
# 📄 v5.9.2 项目执行文件：高斯 EMOS 两级降级 (细化版)
## §1 数据源与时效
**原定**：Reforecast 仅 00Z 起报；5 成员 c00+p01-p04（11/31 成员一律忽略）。6h 窗口 TMAX/TMIN 日极值（完全包含 $\subseteq$ 本地日，弃 3h）。
**[细化时效节点]**：
名义目标 15:00/06:00 LT，归桶 `round_to_nearest_6h`。
*   **最高温真实节点**：`{6h, 30h, 54h}`（30h 为关键验证点）。
*   **最低温真实节点**：`{24h, 48h}`。
*   **模型总量**：20 模型/站 $\times$ 2 站 = 40 组参数。
*   **[v5.9.2 增补] GRIB 变量合并容差约束**：NOAA 历史重预报不同批次间 `tmax_2m` 与 `tmin_2m` 的 `valid_time` 属性字段存在微小差异，`xr.merge` 与 `xr.concat` 必须显式声明 `compat='override'` 与 `coords='minimal'`，避免严格比对误判阻断。
---
## §2 模型架构
**原定**：高斯 EMOS（偏态废弃，Phase 2 可选）。
均值 $\mu = a + b \cdot \bar{T}_{ens}$。
**[细化方差公式]**：
采用**平方参数化**以天然保证非负性：
$$ \sigma^2 = c^2 + d^2 \cdot S_{ens}^2 + \sigma_{clim}^2(d) $$
*   **物理意义**：$c, d$ 可正可负（平方后生效），消除了“负参数即异常”的误判风险。
**[细化 Floor 构建 (Q5)]**：
$\sigma_{clim}(d)$ 定义为**已知偏移量**，不参与优化。
*   **数据口径**：仅站点历史实测（Wunderground），禁用模式数据。
*   **时间口径**：严格 OOS，仅用 **2000-2018** 数据计算，严禁触碰 2019 验证集。
*   **算法定义**：目标日 $d$ 前后各 15 天（31 天窗）$\times$ 19 年 ($\approx$590 样本) 的标准差，逐日平滑曲线。
---
## §3 训练与优化
**原定**：高斯 CRPS 闭式解析解。
**[细化优化配置 (Q6)]**：
*   **优化器**：`scipy.optimize.minimize` 中的 **L-BFGS-B**（比 SGD 更稳）。
*   **正则化**：仅对 $d$ 施加 L2 正则 ($\lambda=10^{-3}$)，$a,b,c$ 不加。
*   **热启动**：初值 $(a,b,c,d) = (0, 1, 0, 1)$，叠加 $O(10^{-3})$ 随机扰动。
---
## §4 推理与降级
**原定**：两级架构：Level 1 高斯 EMOS+Floor $\to$ Level 2 气候学。
**[细化触发条件 (Q1/Q6d)]**：
*   **硬触发（强制降级）**：
    1.  优化器超迭代未收敛。
    2.  参数出现 `NaN` 或 `Inf`。
    *   *(删除原草案中 "c/d 为负" 这一触发条目)*
*   **软触发**：验证集 CRPS 显著劣于气候学（$p<0.05$）。
*   **告警项**：若 $|c|$ 或 $|d| > 10$，记录告警但不强制降级（属过拟合软信号）。
**[细化插值策略 (Q2)]**：
*   **内插**：缺失节点参数 $(a,b,c,d)$ 由相邻节点线性插值生成。
    *   最高温插值节点：`{12, 18, 24, 36, 42, 48}`。
*   **最低温外推（<24h）**：
    *   参数借用：直接使用 24h 模型参数。
    *   方差修正：应用物理衰减 $\sigma_{final} = \sigma_{24h} \times \sqrt{L/24}$。
    *   主导权：极短时效下 EMOS 退居次席，由动态截断层接管。
---
## §5 验收标准
**原定**：三重验收（标准节点 + 插值节点 + 极端压力测试）。MPIW 废弃。
**[细化插值节点验收 (Q3)]**：
采用“留出节点法”。
*   **操作**：将 30h 设为留出节点，用 6h & 54h 插值重建虚拟模型。
*   **双断言标准**：
    1.  精度守恒：$\text{CRPS}_{virtual} \le 1.05 \times \text{CRPS}_{real}$。
    2.  校准完好：PIT K-S 检验 $p > 0.05$。
**[细化极端压力测试 (Q4)]**：
*   **样本池（严格 OOS）**：
    *   阈值：基于 **2000-2018** 数据计算的 90th (Warm Tail) / 10th (Cold Tail) 分位数。
    *   样本：仅抽取 **2019 验证集**中超过阈值的样本（严禁包含训练期样本）。
*   **双指标底线**：
    1.  相对技巧：$\text{CRPS}_{model} < \text{CRPS}_{clim}$（必须战胜气候学）。
    2.  覆盖率：90% 置信区间覆盖率 $\ge 80\%$（允许 10% 损失，防止漏报）。
