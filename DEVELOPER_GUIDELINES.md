# 开发工作流程指南

## 🤖 Agent 协作原则

### 1. 沟通模式区分
- **提问模式**：当用户问"如何"、"为什么"、"是什么"等问题时，只解释概念、原理、方案，**不执行任何代码修改或操作**。
- **执行模式**：当用户明确使用"请做"、"实现"、"运行"、"执行"、"创建"、"修改"等动词时，才执行相应的代码修改或操作。
- **确认模式**：对于模糊或不确定的请求，先确认用户意图，再决定进入提问模式还是执行模式。

### 2. 具体案例
| 用户输入 | 正确响应 | 错误响应 |
|---------|---------|---------|
| "这个函数怎么工作？" | 解释函数原理、参数、返回值 | ❌ 直接修改函数代码 |
| "Task 1.1 是什么？" | 解释任务内容、目标、验收标准 | ❌ 开始实现任务 |
| "请实现用户登录功能" | ✅ 开始写代码实现 | 只解释登录功能的原理 |
| "看看这个错误" | 先问："需要我修复这个错误吗？" | ❌ 直接修复错误 |
| "帮我优化这个代码" | ✅ 执行代码优化 | 只解释优化原则 |

### 3. 工作流程
```
用户描述需求 → Agent提供方案 → 用户确认 → Agent执行 → 报告结果 → 用户下一步指示
```

## 🛠️ 技术工作流

### 1. TDD（测试驱动开发）流程
```
红阶段：写测试（用户确认） → 绿阶段：实现功能（用户确认） → 重构阶段：优化代码（用户确认）
```

### 2. 代码修改原则
- **保持向后兼容**：不破坏现有功能
- **渐进式改进**：小步快跑，每步都可验证
- **文档同步更新**：代码修改时更新相关文档
- **测试覆盖**：新功能必须有测试

### 3. 文件管理
- **规范命名**：`snake_case.py`、`kebab-case.md`、`PascalCase.ts`
- **目录结构**：按功能模块组织
- **版本控制**：每次功能完成提交清晰的消息

## 📁 项目结构规范

### 核心目录
```
project/
├── src/                    # 源代码
├── tests/                  # 测试代码
├── docs/                   # 文档
├── scripts/                # 工具脚本
├── configs/                # 配置文件
├── data/                   # 数据文件
└── examples/               # 示例代码
```

### 文件命名约定
- **Python模块**：`module_name.py`
- **测试文件**：`test_module_name.py`
- **配置文件**：`config-name.yaml` 或 `config_name.toml`
- **文档文件**：`topic-name.md`

## 🔄 开发流程

### 1. 需求分析阶段
- 用户描述需求
- Agent澄清模糊点
- 双方确认需求范围
- Agent提供实现方案

### 2. 实现阶段
- Agent按方案逐步实现
- 每完成一个子任务报告进度
- 用户确认每个步骤
- 遇到问题及时沟通

### 3. 测试验证阶段
- 编写单元测试
- 运行集成测试
- 验证功能完整性
- 用户验收测试

### 4. 文档更新阶段
- 更新API文档
- 更新使用指南
- 记录设计决策
- 更新变更日志

## 📝 代码质量标准

### 1. 代码风格
- 遵循项目现有风格
- 一致的命名约定
- 适当的注释和文档字符串
- 合理的函数长度（< 50行）

### 2. 错误处理
- 适当的异常处理
- 有意义的错误消息
- 资源清理（文件、连接等）
- 优雅降级

### 3. 性能考虑
- 避免不必要的计算
- 合理使用缓存
- 批量处理数据
- 异步处理长时间操作

## 🚨 特殊情况处理

### 1. 模糊请求处理流程
```
用户模糊请求 → Agent询问澄清 → 用户澄清 → Agent确认模式 → 执行相应操作
```

### 2. 错误恢复
- 操作失败时提供详细错误信息
- 建议恢复步骤
- 保留现场状态
- 提供回滚方案

### 3. 长时间操作
- 提前告知预计时间
- 提供进度反馈
- 允许用户中断
- 保存中间结果

## 📊 进度跟踪

### 1. 任务状态标记
- ✅ 已完成
- 🔄 进行中  
- ⏸️ 暂停中
- ❌ 已取消
- 🔍 待审查

### 2. 报告格式
```
## 当前状态
- ✅ 已完成：功能A、功能B
- 🔄 进行中：功能C
- 🔍 待审查：功能D

## 下一步计划
1. 完成功能C的实现
2. 测试功能D
3. 更新文档

## 遇到的问题
- 问题1：描述
- 解决方案：方案描述
```

---

## 📅 记录历史

### 2024-01-15 建立沟通原则
**用户明确**：提问时只解释，不行动。要求执行时才写代码。

**具体规则**：
- 用户问问题 → Agent只解释
- 用户要求执行 → Agent才行动  
- 模糊请求 → 先确认意图

**示例**：
- ❌ 用户："这个函数怎么工作？" → 只解释，不修改代码
- ✅ 用户："请实现这个功能" → 写代码
- ❓ 模糊："看看这个错误" → 先问："需要我修复吗？"

### 2024-01-15 项目当前状态
**Task 1.1：Wunderground数据管道** ✅ 已完成
- 完整12字段提取
- SQLite数据库存储
- 断点续传支持
- 批量下载脚本
- 57个单元测试通过

**下一步**：等待用户指示继续Task 1.2或开始实际数据下载。